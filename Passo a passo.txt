ng new projeto

trbalhando com o Guard

instalar bootstrap
  >> npm install  bootstrap

    >> dentro do angular.json
      >> Acrescentar o style
        >> "styles": [
              "src/styles.css",
              "node_modules/bootstrap/dist/css/bootstrap.min.css"
            ],
            "scripts": [
              "node_modules/bootstrap/dist/js/bootstrap.bundle.min.js"
            ]

pra usar a tela de loguin e preciso imporder o formsmodele dentro do model

importar
FormsModule,
    ReactiveFormsModule
no module para poder fazer as verificações
do formulario

no
html em cima do imput usar o required


criar o guard

ng g guard auth/auth

pergunta ser feita
  >> canActivate
